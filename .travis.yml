language: c

sudo: required

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install libboost-all-dev

script:
  - autoreconf -vi
  - ./configure --with-tcl=/usr/lib/tclConfig.sh --with-pgtcl
  - make clean
  - make
  - sudo make install
